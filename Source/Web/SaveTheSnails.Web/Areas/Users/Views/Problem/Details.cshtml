@model SaveTheSnails.Data.Models.Problem

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

@foreach (var item in Model.Pictures)
{
    <img src="@Url.Action("GetPicture", "Problem", new { id = item.ID })" alt="@item.FileName" />

}

